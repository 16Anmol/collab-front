// WebRTC Meeting Room — integrates NexMeet WebRTC into CoHustle
import React from 'react';
import MeetEntry from '../meet/pages/MeetEntry';

const MeetRoom: React.FC = () => {
  return <MeetEntry />;
};

export default MeetRoom;
